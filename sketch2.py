# Imported the required libraries from the sketchpy library
from sketchpy import library

# For this sketch we took inbuild sketch module of IronMan as ASCII format.
myObject = library.ironman_ascii()

# run the sketch program by using draw function.
myObject.draw()
